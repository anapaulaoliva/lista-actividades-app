import { e2e } from '../support/constantes';

describe('Eliminar una Actividad', () => {
    it('Verificar que se puede eliminar una actividad existente', () => {
    // Visitar la pagina principal
      cy.visit('/')
    cy.validacionInicial()
    
    })
  })
